{
  "type": "module",
  "devDependencies": {
    "eslint": "^9.4.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "prettier": "3.3.2"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "src/**/*.js": [
      "prettier --write --ignore-unknown",
      "eslint --max-warnings=0 --fix"
    ]
  }
}
